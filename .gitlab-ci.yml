cache:
  paths:
    - .m2/repository

stages:
  - clean
  - test
  - install

clean:
  stage: clean
  script:
    - set JAVA_HOME=$JAVA_HOME
    - echo $JAVA_HOME
    - mvn clean
  only:
    - develop

test:
  stage: test
  script:
    - set JAVA_HOME=$JAVA_HOME
    - echo $JAVA_HOME
    - mvn test
  only:
    - develop

install:
  stage: install
  script:
    - set JAVA_HOME=$JAVA_HOME
    - echo $JAVA_HOME
    - mvn install -DskipTests
  only:
    - develop
